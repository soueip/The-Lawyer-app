import 'package:flutter/material.dart';

class Offre extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // Add your Blog screen content here
      body: Center(
        child: Text('offre Screen'),
      ),
    );
  }
}
